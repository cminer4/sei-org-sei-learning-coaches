# Code Review: SEI-53 PR readiness and Azure deployment documentation

**Branch**: `SEI-53-pr-readiness-azure-deployment`  
**Spec**: [specs/features/SEI-53-pr-readiness-azure-deployment.md](../features/SEI-53-pr-readiness-azure-deployment.md)  
**Compared to**: `main` (includes commits `e206f29`, `95d83d8`)  
**Files changed**: 8  
**Lines**: +414 / -28  

---

## Executive summary (for leadership)

This work makes the repo **easier to hand off to sei-org**: environment variables are documented in one place, README explains **Azure**, **Entra redirect URIs**, and the **ElevenLabs Custom LLM** URL pattern, and there is a **repeatable checklist** (automated script plus manual 401 curls). Nothing here changes product behavior; it reduces **deployment mistakes** and **review friction**.

**Verdict**: **Ready to merge**, with one **scope** note (see Concerns) if the branch mixes unrelated files.

---

## What is working well

- **Single source of truth for env**: `.env.example` is grouped by concern with clear comments and production hints, which cuts down “works on my machine” incidents.
- **Operational clarity**: README ties `AUTH_URL` to local vs Azure App Service, explains `KNOWLEDGE_PROVIDER` swap without rewriting the app, and documents Custom LLM as **Bearer secret** (not user login), matching SEI-51 security intent.
- **Repeatable gates**: `npm run pr:readiness` runs lint, production build, unit tests, and the Supabase import boundary check, so reviewers get predictable signals in CI or locally.
- **Stakeholder handoff**: Open dependencies point to [bootstrap-summary.md](../../bootstrap-summary.md) instead of duplicating owners; PR template calls out **Antonio** for Entra redirect URIs.
- **Honest manual steps**: README explains that Custom LLM **401** checks need `INTERVIEW_COACH_CUSTOM_LLM_API_KEY` set, or the route returns **500** (avoids false “failures” during QA).

---

## Constitution compliance

| Area | Status | Notes |
|------|--------|--------|
| Tech stack | OK | Docs only; no stack drift |
| Directory contract | OK | `scripts/` for automation; specs under `specs/features/` |
| SDD / Linear | OK | Spec + implementation log present; ticket SEI-53 in frontmatter |
| TDD | N/A | Documentation and shell script; no new application logic requiring unit tests |
| Code style | OK | Shell script is small and readable |

**Scope note**: Latest commit on the branch (`95d83d8`) adds **SEI-52 review** under `specs/SEI-52-port-admin-kb-prompt-control/`. That is useful but **not part of SEI-53**. Prefer **splitting** that file to an SEI-52-only PR or documenting in the PR body so sei-org reviewers are not confused.

---

## Design review (documentation UX)

- **Contributor journey**: Flow is clear: copy `.env.example` → read Deployment and Azure → run `pr:readiness` → manual curls.
- **No UI changes**: N/A for end users.

---

## Test coverage

- **Automated**: `pr:readiness` runs existing **Vitest** suite and **ensure:build**; no new tests were required for markdown.
- **Manual**: 401 curl matrix is documented; **not** executed in CI (needs running server and cookies), which matches the spec.

**Gap (low risk)**: If CI runs on an image **without `rg` (ripgrep)**, `scripts/pr-readiness.sh` could fail. Most developer machines and Linux CI images have it; **verify** in your pipeline or add a fallback (`grep -R`).

---

## Security (business risk)

- **Good**: `.env.example` uses placeholders; Custom LLM key is described as server-only; no secrets added to the repo.
- **Good**: PR template reminds **Antonio** to register redirect URIs so users are not stuck at login after Azure cutover.
- **Low risk**: Documented curl examples hit **localhost** only; no production URLs with secrets in docs.

---

## Performance

- **N/A**: Documentation and a short shell script; no runtime impact.

---

## Blockers (must fix before merge)

**None**, assuming the team accepts the branch contents.

---

## Concerns (should address)

1. **Mixed ticket scope**  
   **Issue**: SEI-52 review doc landed on the SEI-53 branch.  
   **Why it matters**: Reviewers may think SEI-53 includes SEI-52 code changes.  
   **Fix**: Move the SEI-52 review to its own branch/PR, or state clearly in the PR title/description that SEI-53 is docs-only plus that file.  
   **Time**: 5–15 minutes.

2. **`rg` dependency**  
   **Issue**: `pr-readiness.sh` requires ripgrep.  
   **Why it matters**: CI could fail on minimal images.  
   **Fix**: Document `brew install ripgrep` / install in CI, or use `grep -r` with compatible flags.  
   **Time**: 15–30 minutes if you harden CI.

---

## Suggestions (nice to have)

- Add **`pr:readiness`** to CI (GitHub Actions or Azure Pipelines) so every PR runs the same bar automatically.
- Optional: **`npm run pr:readiness:full`** that starts a test server and runs 401 curls (heavier; often overkill for MVP).

---

## Comparison to spec

| Requirement | Met? |
|-------------|------|
| FR-001 `.env.example` complete | Yes (aligned with codebase `process.env` audit at implementation time) |
| FR-002 README Azure / `KNOWLEDGE_PROVIDER` | Yes |
| FR-003 ElevenLabs Custom LLM URL + Bearer | Yes |
| FR-004 Checklist + script + tests + grep + 401 docs | Yes (401 manual) |
| FR-005 Suggested commit message | Yes (implementation log) |
| FR-006 bootstrap-summary reference | Yes |
| SC-001–SC-005 | Met (manual 401 left to human; documented) |

---

## Code quality (maintainability)

- **Readability**: README sections are scannable; script is short.
- **Duplication**: Minimal; Deployment section references reviewer note for knowledge swap.
- **Drift risk**: When new `process.env` keys are added in code, **someone must update `.env.example`** (NFR-002); consider a one-line note in CONTRIBUTING or GUIDE if drift becomes common.

---

## Overall assessment

**Status**: **Ready to merge** (prefer clarifying SEI-52 review file placement).

**Summary**: Strong documentation and automation for sei-org handoff; low risk; small process cleanup on branch scope.

**Confidence**: **High** that this improves PR readiness without breaking the app.

**Recommendation**: Merge after resolving whether the SEI-52 review belongs on this PR or a separate one.

---

## Reviewer

Generated for `/review SEI-53` (2026-04-20).
