/**
 * SPIN score-session prompts are not used by Assessment Coach. Kept as a short stub for parity with ported lib list.
 */
export const FALLBACK_RUBRIC = `Evaluation criteria fallback (Assessment uses /api/assessment-summary, not SPIN scoring).`;
